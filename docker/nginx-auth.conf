# nginx-auth.conf — Activé dynamiquement par docker-entrypoint.sh
# quand OMNIBANK_PASSWORD est défini. Ce fichier est inclus par nginx.conf.
auth_basic "OmniBank Local";
auth_basic_user_file /etc/nginx/.htpasswd;
